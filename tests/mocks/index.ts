import { BOARDS } from './boards.mock';
import { COMMON } from './_common.mock';
import { COLUMNS } from './columns.mock';
import { CARDS } from './cards.mock';

export const MOCKS = { COMMON, BOARDS, COLUMNS, CARDS };
